import { TopBar, Footer, ActionBar, MainViewContainer } from './';

export function Layout() {
    return (
        <>
            <TopBar />
            <ActionBar />
            <MainViewContainer />
            <Footer />
        </>
    );
}
